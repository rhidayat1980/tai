defmodule Tai.Accounts.Adapters.BitmexTest do
  use ExUnit.Case
  doctest Tai.Accounts.Adapters.Bitmex

  # test "balance returns the total value of the account including active positions" do
  #   assert Tai.Adapters.Bitmex.balance(api_key: "foo", secret: "bar") == 4.2
  # end
end
